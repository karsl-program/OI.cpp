#include <bits/stdc++.h>

namespace RunCode {
    void solve () {
        std :: string a;
        for (size_t i = a.size() - 1; ~i; i --) std :: cout << i;
        std :: cout << "end";
    }
}

int main () {
    RunCode :: solve ();

    return 0;
}

/*

Programming is the art of telling another human being what one wants the computer to do.    -- Donald Knuth

*/